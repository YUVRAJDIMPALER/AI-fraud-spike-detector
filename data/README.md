# Data notes

Place the original PaySim CSV in `data/raw/`.

The simulation layer injects synthetic fraud and genuine-demand spikes on top of this real background. The resulting labeled buckets are written to `data/processed/buckets_labeled.csv`.
